# RCV2-07 工程级执行提示词

任务：V2 高保真视觉、交互与响应式冻结

目标：把用户提供的视觉参考转成可在现有脉图外壳中实现、可截图验收、且严格遵守 V2 API/ViewModel 契约的多运动记录中心设计规范。

输入摘要：

- 用户参考文件：`/Users/fanglei/Downloads/sports_records.html` 和 `/Users/fanglei/Desktop/截屏2026-07-14 10.52.59.png`。
- 参考视觉优点：暗色沉浸、顶部运动页签、左侧纪录卡、右侧大图、底部摘要卡、蓝色高亮、圆角和层级清晰。
- 参考视觉不可继承项：外部 CDN、头像/通知无关元素、表现指数、伪年度数据、硬编码 PB 列表、前端计算趋势。
- `RCV2-06` 已冻结 Catalog/Records/Detail/History/Curve/Candidate ViewModel；前端必须只消费后端字段。

前置依赖：`RCV2-06`。

文件范围：

- 可写：本提示词、`docs/records_center_v2_rcv2_07_visual_interaction_contract.md`、完成报告、滚动摘要、V2 任务清单。
- 禁止：前端代码、业务代码、API contract JSON、真实库、打包产物。

冻结契约：

- 运动页签由 Catalog 驱动；不得硬编码未完成运动为正式可用。
- 不显示表现指数、伪年度数据或前端推导的总提升。
- 所有纪录值、趋势点、轴方向、候选状态、灰态文案均来自后端 ViewModel。
- 不依赖外部 CDN；图标、CSS、图表能力必须使用现有项目可用资源或本地实现。
- 新视觉必须嵌入现有脉图“运动生涯”外壳，不引入独立产品导航。

实施步骤：

1. 提炼参考视觉可继承与必须舍弃的部分。
2. 冻结页面信息架构：顶部运动页签、二级状态页签、左侧分组/纪录列表、右侧主图/详情、底部摘要。
3. 分别冻结跑步、骑行、徒步、游泳、越野的图表模式、卡片内容和空态。
4. 冻结 Loading/Empty/Partial/Candidate/Rebuilding/Error/Validation Required 状态。
5. 冻结桌面、1100px、980px、720px、窄窗口响应式布局。
6. 冻结颜色、间距、字体、圆角、图标、文本溢出、键盘、读屏和 reduced motion。
7. 写截图验收基线和后续实现测试计划。
8. 更新滚动摘要和任务状态。

非目标：

- 不实现前端代码。
- 不引入外部 CDN 或新增资源。
- 不调整 API 契约。
- 不打包。

验证：

- 文档检查：必须包含视觉参考取舍、断点、状态、五运动、API 字段约束、无 CDN、无表现指数。
- 运行 golden fixture 测试作为任务链基础回归。

完成定义：

- `RCV2-32` 至 `RCV2-35` 可以按本文实现前端，不需要重新解释视觉风格、布局、状态或响应式规则。
