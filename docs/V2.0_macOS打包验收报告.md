# 脉图 V2.0 macOS 打包验收报告

更新时间：2026-08-03  
状态：REL-06 至 REL-08 已完成

## 1. 产物

| 架构 | DMG | SHA-256 | 大小 |
| --- | --- | --- | --- |
| arm64 | `dist/v2.0/FitVault-V2.0-macOS-arm64.dmg` | `65979523d013b02f4bd7092c17a7066e67f0dd2816bde83a39eee2496a2ee744` | 109,944,000 bytes |
| x86_64 | `dist/v2.0/FitVault-V2.0-macOS-x86_64.dmg` | `1916f18e4fcf373c71870dae605842938ba919a4f2bfbdc3160df580c1fb8ace` | 112,940,133 bytes |

两个 bundle 的 `CFBundleShortVersionString` / `CFBundleVersion` 均为 `2.0.0`。
DMG 均通过 `hdiutil verify`，并包含 `Applications -> /Applications` 快捷方式。

## 2. 产物内容审计

- 主程序架构与目标一致；随包 Node runtime 为对应架构，npm 入口存在。
- `track.html`、帮助文档、Cesium 1.143、Cesium worker/wasm、肌群热力图
  资源及许可证、Garmin/COROS Skill 目录与 ZIP 均存在。
- 每个 bundle 的 Resources 文件数为 5793，Cesium 文件数为 392，Node 文件数为
  4702。
- 未发现测试目录、pytest、真实 FIT、数据库、日志或凭据文件。
- portable dependency manifest 只记录 `<bundled-python>` 和 bundle-relative
  `node/bin/*` 路径，不暴露构建机绝对路径。
- x86_64 Python/OpenSSL dylib 已用 `scripts/sanitize_macos_bundle_paths.py`
  脱敏并重新 ad-hoc 签名；最终无 `/Users/fanglei` 或 `.pyenv/versions` 命中。

## 3. 已完成验证

- 全量测试：`3491 passed, 5 skipped, 262 subtests passed`。
- 打包诊断、Python 3.12 runtime、两架构诊断通过；聚焦资源/诊断测试
  `22 passed`。
- 新装隔离启动：两架构均能创建临时 `~/.fitvault` 数据目录和数据库，
  `PRAGMA integrity_check` 为 `ok`。

## 4. 终验结论

用户已手工完成并确认通过以下验收：

- FIT 导入并打开活动详情；
- 力量训练动作组与肌肉热力图；
- 轨迹分析标准/真实地形与回退；
- V1.2.0 覆盖安装与幂等检查。

V2.0 macOS 发布链路可按既定网盘分发边界交付。

## 5. 分发边界

本次按网盘分发，不包含 Developer ID 签名、Apple 公证或 Gatekeeper 免提示。
Windows/WiX/MSI 在另一台 Windows 打包机上独立完成。
