首要约束：所有代码修改、功能开发操作必须严格在项目已定义的架构契约约束范围内执行，不得违反项目的技术架构规范、目录结构约定及编码标准；必须遵守以下核心架构契约：FIT 字段不得直接透传 UI；Resolver 是唯一语义翻译层；DB 只存系统标准字段；UI 只能消费 DB/API 契约字段且禁止 fallback 推断；所有字段必须兜底，禁止 None/null/undefined/“/” 污染展示；高风险接口必须具备鉴权、确认或等价安全门禁；所有接口响应必须向 `{code,msg,data,traceId}` 统一响应结构演进；不得新增未文档化接口、未登记字段或本地孤立 schema。

任务编号：T003

任务名称：LLM 配置敏感字段脱敏

目标文件：`main.py`、`llm_backend.py`

定位行号：`main.py:L2415-L2420`、`main.py:L2463-L2503`

执行目标：修复 `get_llm_config()` 原样返回配置导致 `api_key` 暴露给前端的问题；返回给前端的配置中不得包含完整密钥，只允许返回 `has_api_key`、脱敏尾号或空字符串。

执行要求：保持 `test_llm_config()` 作为测试后持久化网关；保存逻辑仍可接收用户输入的新 `api_key`；读取逻辑必须脱敏；不得在日志、异常、响应中输出完整密钥；如需调整 `llm_backend.py`，应优先新增脱敏辅助函数而非改变配置文件格式。

禁止事项：禁止把真实密钥写入前端状态；禁止在错误响应中拼接密钥；禁止删除现有配置兼容能力。

必读文件：`ARCHITECTURE.md`、`docs/DIR_SPEC.md`、`main.py`、`llm_backend.py`、`track.html`

允许修改范围：允许修改 `main.py`、`llm_backend.py` 与必要的前端配置读取适配；不得改变配置文件路径和现有测试后保存网关语义；不得输出或记录真实密钥。

推荐测试命令：`python3 -m pytest test_import.py`

完成后报告格式：输出“脱敏策略 / 兼容性说明 / 敏感信息检查结果 / 测试命令与结果 / 未覆盖风险”五段；必须明确 `get_llm_config()` 不返回完整 `api_key`。

验收标准：调用 `get_llm_config()` 时响应中不存在完整 `api_key`；已有配置仍可用于 `call_llm()`；测试失败时不破坏旧配置且不泄漏密钥。
